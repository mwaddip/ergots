# PLAN — npm-org rename → @ergots/* (COMPLETE 2026-05-19)

✅ **COMPLETE 2026-05-19.** Rename landed in 18 commits on `master` (commits `f14af18` through `fe4d14b` plus 3 fix-commits).

## Substitutions executed

| Old | New | Scope |
|---|---|---|
| `@mwaddip/ergots-proof` | `@ergots/nipopow` | npm package name + repository.directory |
| `@mwaddip/ergots-avltree` | `@ergots/avltree` | npm package name |
| `@mwaddip/ergots-ergoscript` | `@ergots/ergoscript` | npm package name |
| `@mwaddip/ergots-gossip` | `@ergots/gossip` | historical (rejected/never-shipped package) |
| `@mwaddip/ergots-*` | `@ergots/*` | wildcard in CLAUDE.md naming-convention statement |
| `@mwaddip/ergots` (bare org) | `ergots` | umbrella project label (per user decision) |
| `packages/proof/` | `packages/nipopow/` | directory rename via `git mv` |
| `facts/proof.md` | `facts/nipopow.md` | facts file rename via `git mv` |

## Verification (final)

- `npm test --workspaces`: 3103 tests PASS (avltree 140, ergoscript 2658, nipopow 305) under both `node` and `jsdom`.
- `npx tsc --noEmit`: clean per package.
- `cargo build` + `cargo test` + `cargo run --release`: green; fixtures regenerated byte-identical (determinism check passed).
- `git ls-files | xargs grep '@mwaddip/ergots\|packages/proof\|facts/proof\.md'`: only **2 intentional historical references** remain in tracked files:
  - `README.md` line 11: "was published as `@mwaddip/ergots-proof@0.1.0` pre-rename" (historical context)
  - 3 files referencing the literal spec filename `docs/specs/2026-05-18-ergots-avltree-package-design.md` (spec file's name; not renamed this session)

## Publish posture

In-repo rename only. NO `npm publish` executed this session per user decision.

- `@ergots/nipopow` — local v0.1.0, ready to publish. (Old `@mwaddip/ergots-proof@0.1.0` still live on npm; user has not yet chosen deprecate-vs-leave-live.)
- `@ergots/avltree` — local v0.1.0, ready to publish.
- `@ergots/ergoscript` — local v0.2.x, pre-publish (v0.3.0 publish post-rename + post-facts-split + post-2g.6 still queued).

## Next

This session continues with "further work on the nipopow module" per user — specifics to surface mid-session.

Then still queued (future sessions):
- **Phase 2h-b**: ergoscript ↔ avltree integration spec (wires 11 SAvlTree.* method handlers + LastBlockUtxoRootHash arm in `@ergots/ergoscript` to call into `@ergots/avltree`)
- **Phase 2i**: predefs slice (DecodePoint ~21% mainnet, SubstConstants ~21%, CalcBlake2b256 ~3.5%, byte-array conversions, hash predefs)
- **Phase 2j**: cost accounting validation — load-bearing for consensus per umbrella spec
- **`@ergots/ergoscript@0.3.0` publish** (post-rename, post-facts-split, post-2g.6)
- **Tier 3 method-handler cleanup** from 2g.6: `SColl.flatten` + `SGroupElement.getEncoded`
