# 2j-pre fix-2 Layer 3 smoke findings (2026-05-22)

## Spec success criterion

> "the harness advances PAST the `missing-utxo at h=3850` halt site."

## Result: criterion met — fix-2 seeding works

Walks from `h=1` against a FRESH sidecar (deleted prior fix-1
checkpoint+sidecar per reviewer M5). The shim's startup correctly logs:

```
shim: network=Mainnet, computed 3 genesis box(es) for sidecar seeding
sidecar opened at height 0
```

The founders box (`5527430474b673…3c88aeda`) is now present in the
sidecar from the seed; at h=3850 tx#2 in#0 the standard walker REMOVE
resolves it cleanly. **No more shim `missing-utxo` halt at h=3850.**

## Smoke against bootstrap-data (mainnet tip 1,790,510)

| metric | value |
|---|---|
| network flag | `--network mainnet` |
| start | 1 |
| max | 10000 |
| validated to | 3849 |
| blocks | 3849 |
| txs | 3886 |
| boxes validated | 7772 |
| spends validated (evaluate + verifySignature) | 3969 |
| elapsed | 27.5 s (similar to fix-1 baseline) |
| halt | h=3850, phase `evaluate`, errorCode `evaluate-threw` |

## New halt site (next fix-list item for 2j proper)

The halt is now at the SAME height (3850) but in the FOURTH phase
(`evaluate`) on the SAME tx — tx#2 input 0 — spending the same
founders box. After fix-2 the REMOVE succeeds; the harness then runs
`evaluate` on the founders box's ErgoTree (a 2-of-3 multisig script
using `Atleast`):

```
phase: evaluate
errorClass: HarnessError
errorCode: evaluate-threw
message: "evaluate threw non-EvalError at tx 2, input 0: exprTpe: variant
          'Atleast' not yet supported (add an arm in expr-tpe.ts when its
          parser lands)"
location:
  txIndex: 2
  inputIndex: 0
  spentBoxId: 5527430474b673e4aafb08e0079c639de23e6a17e87edd00f78662b43c88aeda
  ergoTreeHex: 100e040004c094400580809cde91e7b001...d5040800
```

The spent box is the same `5527430474b673...` (founders) box — its
ErgoTree is the foundation 2-of-3 multisig, which uses `Atleast`. Our
`@ergots/ergoscript` `expr-tpe.ts` lacks an arm for the `Atleast`
variant; the throw comes from there, not the evaluator proper.

This is fix-list item 3 for 2j proper:
- **fix-3 (TS):** `packages/ergoscript/src/wire/expr-tpe.ts` missing an
  arm for the `Atleast` Expr variant. The parser already produces
  `Atleast` (it's listed in the 67/67 implementable arms and has an
  eval arm shipping in 2g-combinators per `facts/ergoscript-sigma.md`),
  so this is just an `exprTpe` projection gap.
- Reproducer: walk past h=3850 with the fix-2 shim binary; first
  `evaluate` pass on tx#2's input.

## Pre-fix-2 state (fix-1 baseline)

Same metrics as above through h=1..3849, but halted at h=3850 in phase
`shim` with `errorCode: missing-utxo` for box `5527430474b673…`. Fix-2
moves the halt past the shim phase into the evaluate phase, exactly
as designed.

## Delta

| | pre-fix-2 (fix-1 baseline) | post-fix-2 |
|---|---|---|
| h=3850 halt phase | `shim` | `evaluate` |
| h=3850 errorCode | `missing-utxo` | `evaluate-threw` |
| founders box in sidecar | NO | YES (seeded) |
| founders box REMOVEd at h=3850 | n/a (halted earlier) | YES |
| Next halt site | n/a | tx#2 input 0 `expr-tpe.ts:Atleast` |

## Defensive assertion fired correctly

The startup-time hard-coded expected-id check (per Decision 6) ran
clean — all 3 mainnet genesis boxes match the hard-coded expected ids
from `ergo-node-rust/src/main.rs:3092-3096`. No mismatch surfaced.

## Artifacts

- `/tmp/t8-fix2-{sidecar,checkpoint,error-report}.{redb,json}` — fix-2
  smoke output. Checkpoint at `lastValidatedHeight: 3849`.
- Gitignored by location (under `/tmp/`).
