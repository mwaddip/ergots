# 2j-pre fix-3 Layer 3 smoke findings (2026-05-22)

## Spec success criterion (reviewer M2 calibrated)

> "Walks past h=3850; new halt LIKELY within a small number of blocks given the 14 remaining gaps."

## Result: STRETCH OUTCOME — tip reached at h=10000

Walks from a FRESH sidecar with `--max-height 10000`. **10,000 contiguous blocks validated end-to-end** across all four phases (header / output-roundtrip / evaluate / verifySignature). No halts surfaced.

```
shim: network=Mainnet, computed 3 genesis box(es) for sidecar seeding
sidecar opened at height 0
Walking 1..10000 (tip=1790510, network=mainnet)
Tip reached at height 10000.
```

## Stats (post-fix-3 walk to h=10000)

| metric | value |
|---|---|
| start | 1 |
| max | 10000 |
| validated to | 10000 (tip-reached) |
| blocks | **10,000** (2.6× fix-2's 3,849) |
| txs | 10,405 |
| boxes validated | 20,857 |
| spends validated (evaluate + verifySignature) | 11,277 |
| elapsed | 109.8 s |
| halt | none — clean tip-reached |

## Delta vs prior fixes

| | pre-fix-1 (2j-pre T12) | post-fix-1 | post-fix-2 | **post-fix-3** |
|---|---|---|---|---|
| blocks validated end-to-end | 0 | 3849 | 3849 | **10000** |
| first halt | h=1 (sbox-no-size) | h=3850 (shim missing-utxo) | h=3850 (evaluate Atleast) | none up to h=10000 |
| evaluate + verifySignature paths tested | n/a | 3849 spends | 3969 spends | **11,277 spends** |

## Implication

Fix-3 closed the Atleast exprTpe gap that fix-2 surfaced. The reviewer's M2 prediction (new halt within a few blocks on `CalcSha256`, `ExtractBytes`, `SigmaPropIsProven`) did not materialize at heights 3850..10000 — the foundation multisig at h=3850 was the only block in that range that exercised any of the 14 remaining exprTpe gaps.

The library's `evaluate` + `verifySignature` paths handle real mainnet boxes through h=10000 cleanly with **no observed library divergence**.

## Deeper smoke (background follow-up) — h=20644 clean before SIGTERM

A follow-up background smoke with `--max-height 100000` ran concurrently. It walked through **h=20644 cleanly** before being SIGTERM-killed by the operator (to free the redb file lock for the integration test suite). No naturally-occurring halt surfaced in heights 1..20644 — the kill was administrative, not a chain divergence.

This is a stronger lower bound on the library's pre-existing handling of real mainnet shapes than the 10000-block primary smoke. The next halt (if any) lives at h > 20644; finding it requires a longer uninterrupted smoke (2j proper's territory).

## What this means for the fix-list

- ~~Fix-3 (Or/Xor/Atleast exprTpe arms)~~ — RESOLVED.
- ~~Fix-2 (genesis-box seeding)~~ — RESOLVED.
- ~~Fix-1 (sbox-ergo-tree-no-size)~~ — RESOLVED.

**No new fix-list item surfaced at heights ≤ 10000.** The 14 other unhandled exprTpe arms (CalcSha256, ExtractBytes, SigmaPropIsProven, etc.) plus the 5+ eval-side `'not-implemented-yet'` arms remain as latent gaps — they'll surface in 2j proper's deeper smoke walks.

## T6 implication

The fix-2 T9 halt-path snapshot pinned the halt at h=3850 phase 'evaluate' errorCode 'evaluate-threw'. With fix-3 making heights 1..10000 walk cleanly, **that snapshot is broken** (the harness no longer halts at h=3850; it tip-reaches).

T6 must either:
- (a) Bump --max-height much higher to find a new halt; OR
- (b) Restructure the test as a "clean tip-reach" pass with a small --max-height like 100; OR
- (c) Use deliberate fault injection (e.g., truncated store-path → ShimError startup halt — already covered by the other `it()` in halt-path.test.ts).

Option (b) is the path of least churn; the test then becomes the positive counterpart of the startup-halt scenario and locks in the post-fix-3 clean-walk behavior. T6 commits this.

## Artifacts

- `/tmp/t-fix3-sidecar.redb` — fresh sidecar with all 3 genesis boxes seeded + 10000 blocks walked.
- `/tmp/t-fix3-checkpoint.json` — `lastValidatedHeight: 10000, tipReachedAt: 2026-05-22T09:28:59.787Z`.
- No `error-report.json` (clean walk; tip-reach deletes any stale sidecar).

All gitignored by location (under `/tmp/`).
