# 2j-pre fix-1 Layer 3 smoke findings (2026-05-22)

## Spec success criterion (reviewer M2 applied)

> "the harness advances PAST the `sbox-ergo-tree-no-size` halt site."

## Result: stretch outcome — far past the criterion

3849 blocks validated end-to-end against the bootstrap-data snapshot
at `/tmp/ergots-2j-pre-smoke-data/modifiers.redb` (mainnet tip
1,790,510). All four validation phases (header / output-roundtrip /
evaluate / verifySignature) passed for every block.

## Smoke 1: h=1..5 (sanity)

| metric | value |
|---|---|
| start | 1 |
| max  | 5 |
| validated to | 5 (tip-reached) |
| blocks | 5 |
| txs | 5 |
| boxes validated | 10 |
| spends validated (evaluate + verifySignature) | 5 |
| elapsed | 88 ms |
| halt | none — clean tip-reached |

## Smoke 2: h=1..4000 (deep walk)

| metric | value |
|---|---|
| start | 1 |
| max  | 4000 |
| validated to | 3849 |
| blocks | 3849 |
| txs | 3886 |
| boxes validated | 7772 |
| spends validated (evaluate + verifySignature) | 3969 |
| elapsed | 27.5 s |
| halt | h=3850, phase `shim`, errorCode `missing-utxo` |
| halt detail | box `55274304…3c88aeda` not in UTXO index — the previously-known shim walker bug (fix-list item 2) |

The halt at h=3850 is EXACTLY fix-list item 2 (the shim's
`block_walker.rs:535` deterministic UTXO accounting bug). NOT a new
library issue; NOT introduced by fix-1.

## Pre-fix-1 state (2j-pre T12 baseline)

Every smoke attempt halted at output-roundtrip on the FIRST output of
the FIRST tx with errorCode `sbox-parse-failed` (underlying
`sbox-ergo-tree-no-size`). Heights tried: 1, 1000, 3849. None
validated even a single block.

## Delta

| | pre-fix-1 (2j-pre T12) | post-fix-1 (2j-pre fix-1 T7) |
|---|---|---|
| blocks validated at h=1..5 start | 0 (halt at h=1) | 5 (tip-reached) |
| deepest validated walk | 0 | 3849 |
| first downstream halt | n/a (immediate parse halt) | h=3850 (shim walker) |

## Implication for next phase

Fix-list item 1 (sbox-ergo-tree-no-size, this fix-1 spec) is fully
RESOLVED — no observed downstream halt in the library through 3849
contiguous blocks. The evaluate + verifySignature paths handle real
mainnet boxes through this depth cleanly.

Fix-list item 2 (shim walker missing-utxo at h=3850) is unchanged and
remains the next focused-fix spec target.

Beyond h=3850, the library's behavior at higher heights is untested
by this smoke walk. The 27.5 s elapsed for the first 3849 blocks
suggests deeper smokes are feasible once fix-2 unblocks the shim
walker.

## Artifacts

- `/tmp/t7-fix1-{sidecar,checkpoint,error-report}.{redb,json}` — smoke 1.
- `/tmp/t7-fix1b-{sidecar,checkpoint,error-report}.{redb,json}` — smoke 2.

Both gitignored by location (under `/tmp/`).
